/** Runs before first paint — avoids homepage flash before splash; includes a no-JS/React failsafe. */
export const SPLASH_BOOT_SCRIPT = `(function(){try{var s=sessionStorage.getItem('hasSeenSplash')==='true';var r=document.documentElement;r.classList.toggle('splash-complete',s);r.classList.toggle('splash-pending',!s);if(!s){setTimeout(function(){if(!r.classList.contains('splash-complete')){r.classList.remove('splash-pending','splash-exiting','splash-react-ready');r.classList.add('splash-complete');try{sessionStorage.setItem('hasSeenSplash','true');}catch(e){}document.body.style.overflow='';}},4500);}}catch(e){document.documentElement.classList.add('splash-complete');}})();`;
